import 'package:flutter/material.dart';

class TicketManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Gestion des tickets")),
      body: Center(child: Text("Page de gestion des tickets en construction...")),
    );
  }
}
