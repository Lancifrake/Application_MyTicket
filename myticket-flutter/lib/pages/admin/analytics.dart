import 'package:flutter/material.dart';

class AnalyticsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Analyse des ventes")),
      body: Center(child: Text("Page d'analyse des ventes en construction...")),
    );
  }
}
